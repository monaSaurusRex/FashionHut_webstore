export interface Product {
    id: number,
    title: string, //product name
    price: number,
    description: string,
    category: string,
    image: string, //imageUrl

    // product specifications
    // brand: string;
    // colour: string;
    // size: string;
}